openapi: 3.1.0
info:
  title: Iroha API
  version: 0.4.0
  description: |
    Active-development contract for Iroha's private data cockpit.
    /api/v1 is still pre-1.0 and may evolve between minor releases. This
    document is the implementation target for release 0.4.0.
servers:
  - url: /
    description: Same-origin or reverse-proxy deployment
tags:
  - name: system
  - name: imports
  - name: activities
  - name: sleep
  - name: daily
  - name: expenses
  - name: media
  - name: control-room
paths:
  /healthz:
    get:
      tags: [system]
      responses:
        "200":
          description: Server is alive.
          content:
            application/json:
              schema:
                type: object
                required: [status]
                properties:
                  status:
                    type: string
                    const: ok
  /api/v1/briefing:
    get:
      tags: [system]
      parameters:
        - $ref: "#/components/parameters/Date"
      responses:
        "200":
          description: Day briefing with versioned sections.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Briefing"
        "400":
          $ref: "#/components/responses/BadRequest"
  /api/v1/raw-files:
    get:
      tags: [imports]
      responses:
        "200":
          description: Most recent raw files.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/RawFile"
    post:
      tags: [imports]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file, source_kind]
              properties:
                file:
                  type: string
                  format: binary
                source_kind:
                  $ref: "#/components/schemas/SourceKind"
                uploaded_via:
                  $ref: "#/components/schemas/UploadSource"
      responses:
        "201":
          description: Raw file stored.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RawFile"
        "200":
          description: File content was already stored; duplicate is true.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RawFile"
        "400":
          $ref: "#/components/responses/BadRequest"
  /api/v1/raw-files/{rawFileId}:
    get:
      tags: [imports]
      parameters:
        - $ref: "#/components/parameters/RawFileId"
      responses:
        "200":
          description: Raw file metadata.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RawFile"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/imports:
    get:
      tags: [imports]
      responses:
        "200":
          description: Most recent import jobs.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ImportJob"
    post:
      tags: [imports]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateImport"
      responses:
        "202":
          description: Import job queued.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImportJob"
        "400":
          $ref: "#/components/responses/BadRequest"
  /api/v1/imports/{importId}:
    get:
      tags: [imports]
      parameters:
        - $ref: "#/components/parameters/ImportId"
      responses:
        "200":
          description: Import job status.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImportJob"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/activities:
    get:
      tags: [activities]
      parameters:
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Cursor"
        - name: sport_type
          in: query
          schema: { type: string }
        - name: started_from
          in: query
          schema: { type: string, format: date-time }
        - name: started_to
          in: query
          schema: { type: string, format: date-time }
        - name: min_distance_m
          in: query
          schema: { type: number }
        - name: max_distance_m
          in: query
          schema: { type: number }
      responses:
        "200":
          description: Activity page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActivityPage"
        "400":
          $ref: "#/components/responses/BadRequest"
  /api/v1/activities/summary:
    get:
      tags: [activities]
      parameters:
        - name: year
          in: query
          schema: { type: string }
        - name: sport
          in: query
          schema: { type: string }
      responses:
        "200":
          description: Activity summary aggregates (totals, by year/month/sport).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActivitySummary"
  /api/v1/activities/routes:
    get:
      tags: [activities]
      responses:
        "200":
          description: Trimmed, privacy-masked GeoJSON route collection for every activity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RouteFeatureCollection"
  /api/v1/activities/{activityId}:
    get:
      tags: [activities]
      parameters:
        - $ref: "#/components/parameters/ActivityId"
      responses:
        "200":
          description: Activity detail.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Activity"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/activities/{activityId}/route:
    get:
      tags: [activities]
      parameters:
        - $ref: "#/components/parameters/ActivityId"
      responses:
        "200":
          description: Ordered route points.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/RoutePoint"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/activities/{activityId}/samplings:
    get:
      tags: [activities]
      parameters:
        - $ref: "#/components/parameters/ActivityId"
      responses:
        "200":
          description: Activity sampling records.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Sampling"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/activities/{activityId}/laps:
    get:
      tags: [activities]
      parameters:
        - $ref: "#/components/parameters/ActivityId"
      responses:
        "200":
          description: Activity laps.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Lap"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/sleep:
    get:
      tags: [sleep]
      parameters:
        - $ref: "#/components/parameters/DateFrom"
        - $ref: "#/components/parameters/DateTo"
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Cursor"
      responses:
        "200":
          description: Sleep session page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SleepPage"
        "400":
          $ref: "#/components/responses/BadRequest"
  /api/v1/sleep/aggregates:
    get:
      tags: [sleep]
      parameters:
        - $ref: "#/components/parameters/Granularity"
        - $ref: "#/components/parameters/DateFrom"
        - $ref: "#/components/parameters/DateTo"
      responses:
        "200":
          description: Sleep aggregate buckets.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SleepAggregateResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
  /api/v1/sleep/{sleepId}:
    get:
      tags: [sleep]
      parameters:
        - $ref: "#/components/parameters/SleepId"
      responses:
        "200":
          description: Sleep session detail.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Sleep"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/sleep/{sleepId}/segments:
    get:
      tags: [sleep]
      parameters:
        - $ref: "#/components/parameters/SleepId"
      responses:
        "200":
          description: Sleep stage segments.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SleepSegment"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/daily:
    get:
      tags: [daily]
      parameters:
        - $ref: "#/components/parameters/DateFrom"
        - $ref: "#/components/parameters/DateTo"
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Cursor"
      responses:
        "200":
          description: Daily summary page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DailyPage"
        "400":
          $ref: "#/components/responses/BadRequest"
  /api/v1/daily/aggregates:
    get:
      tags: [daily]
      parameters:
        - $ref: "#/components/parameters/Granularity"
        - $ref: "#/components/parameters/DateFrom"
        - $ref: "#/components/parameters/DateTo"
      responses:
        "200":
          description: Daily aggregate buckets.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DailyAggregateResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
  /api/v1/expenses:
    get:
      tags: [expenses]
      parameters:
        - $ref: "#/components/parameters/DateFrom"
        - $ref: "#/components/parameters/DateTo"
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Cursor"
        - name: currency
          in: query
          schema:
            type: string
            enum: [JPY, USD, EUR, GBP]
        - name: category
          in: query
          schema:
            type: string
            enum: [food, groceries, transport, shopping, housing, utilities, health, entertainment, subscriptions, work, other]
      responses:
        "200":
          description: Active expense page ordered by occurred date descending.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExpensePage"
        "400":
          $ref: "#/components/responses/BadRequest"
    post:
      tags: [expenses]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateExpense"
      responses:
        "201":
          description: Expense created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Expense"
        "200":
          description: Idempotent retry returned the existing expense.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Expense"
        "400":
          $ref: "#/components/responses/BadRequest"
        "409":
          $ref: "#/components/responses/Conflict"
        "410":
          $ref: "#/components/responses/Gone"
  /api/v1/expenses/{expenseId}:
    get:
      tags: [expenses]
      parameters:
        - $ref: "#/components/parameters/ExpenseId"
      responses:
        "200":
          description: Expense record.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Expense"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "410":
          $ref: "#/components/responses/Gone"
    put:
      tags: [expenses]
      parameters:
        - $ref: "#/components/parameters/ExpenseId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReplaceExpense"
      responses:
        "200":
          description: Expense replaced.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Expense"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "410":
          $ref: "#/components/responses/Gone"
    delete:
      tags: [expenses]
      parameters:
        - $ref: "#/components/parameters/ExpenseId"
      responses:
        "204":
          description: Expense tombstone recorded; repeat deletes are also successful.
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/media:
    get:
      tags: [media]
      parameters:
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Cursor"
        - name: status
          in: query
          schema: { type: string }
        - name: media_type
          in: query
          schema: { type: string }
        - name: family
          in: query
          schema: { type: string }
        - name: completed_year
          in: query
          schema: { type: integer }
      responses:
        "200":
          description: Media library page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MediaPage"
        "400":
          $ref: "#/components/responses/BadRequest"
  /api/v1/media/sync/{connectorId}:
    post:
      tags: [media]
      summary: Queue a configured media connector sync.
      description: |
        Queues a sync for a connector configured by the deployment. Credentials
        are resolved by the worker environment and are never accepted in this
        request body. The pre-release API currently supports `anilist` and
        `bangumi`.
      parameters:
        - name: connectorId
          in: path
          required: true
          schema:
            type: string
            enum: [anilist, bangumi]
      responses:
        "202":
          description: Sync job queued.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Job"
        "400":
          $ref: "#/components/responses/BadRequest"
        "503":
          description: Job dispatcher is unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api/v1/media/events:
    get:
      tags: [media]
      parameters:
        - $ref: "#/components/parameters/DateFrom"
        - $ref: "#/components/parameters/DateTo"
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Cursor"
      responses:
        "200":
          description: Media event page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MediaEventPage"
        "400":
          $ref: "#/components/responses/BadRequest"
  /api/v1/media/aggregates:
    get:
      tags: [media]
      responses:
        "200":
          description: Media aggregate data.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MediaAggregates"
  /api/v1/media/{mediaId}:
    get:
      tags: [media]
      parameters:
        - $ref: "#/components/parameters/MediaId"
      responses:
        "200":
          description: Media detail.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MediaDetail"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/media/resolution-tasks:
    get:
      tags: [media]
      summary: List the media cross-provider resolution inbox.
      description: |
        Dedupe-candidate and progress-conflict tasks the media resolver
        (apps/iroha-imports) writes when it cannot auto-resolve a synced
        item. Defaults to open tasks only.
      parameters:
        - name: status
          in: query
          schema: { type: string, enum: [open, resolved, dismissed] }
      responses:
        "200":
          description: Media resolution tasks.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MediaResolutionTask"
        "400":
          $ref: "#/components/responses/BadRequest"
  /api/v1/media/resolution-tasks/{taskId}:
    patch:
      tags: [media]
      summary: Record the operator's decision on a resolution task.
      description: |
        Closes an open task as resolved or dismissed. This only records the
        decision in resolution_json for a future consumer to act on -- it
        does not merge media rows or apply a progress choice.
      parameters:
        - name: taskId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status: { type: string, enum: [resolved, dismissed] }
                resolution: { type: object, additionalProperties: true }
      responses:
        "200":
          description: Updated resolution task.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MediaResolutionTask"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/tasks:
    get:
      tags: [control-room]
      summary: List personal control-room tasks.
      parameters:
        - $ref: "#/components/parameters/Limit"
        - name: status
          in: query
          schema: { type: string, enum: [open, completed, canceled] }
        - name: due
          in: query
          schema: { type: string, format: date }
      responses:
        "200":
          description: Personal tasks.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Task" }
        "400":
          $ref: "#/components/responses/BadRequest"
    post:
      tags: [control-room]
      summary: Create a personal control-room task.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateTask"
      responses:
        "201":
          description: Task created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
        "400":
          $ref: "#/components/responses/BadRequest"
  /api/v1/tasks/{taskId}:
    patch:
      tags: [control-room]
      summary: Complete or cancel a personal task.
      parameters:
        - $ref: "#/components/parameters/TaskId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status: { type: string, enum: [completed, canceled] }
      responses:
        "200":
          description: Updated task.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/jobs:
    get:
      tags: [control-room]
      summary: List durable system jobs.
      parameters:
        - $ref: "#/components/parameters/Limit"
        - name: kind
          in: query
          description: Exact kind, or comma-separated kinds.
          schema: { type: string }
        - name: status
          in: query
          schema: { type: string }
      responses:
        "200":
          description: Recent durable jobs.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Job" }
        "400":
          $ref: "#/components/responses/BadRequest"
  /api/v1/jobs/{jobId}:
    get:
      tags: [control-room]
      summary: Read one durable system job.
      parameters:
        - $ref: "#/components/parameters/JobId"
      responses:
        "200":
          description: Durable job detail.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Job"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/actions/{action}:
    post:
      tags: [control-room]
      summary: Trigger one named safe action.
      parameters:
        - name: action
          in: path
          required: true
          schema:
            type: string
            enum: [media-sync-anilist, media-sync-bangumi]
      responses:
        "202":
          description: Action queued as a durable job.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Job"
        "400":
          $ref: "#/components/responses/BadRequest"
        "503":
          description: Job dispatcher is unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
components:
  parameters:
    RawFileId:
      name: rawFileId
      in: path
      required: true
      schema: { type: string }
    ImportId:
      name: importId
      in: path
      required: true
      schema: { type: string }
    ActivityId:
      name: activityId
      in: path
      required: true
      schema: { type: string }
    SleepId:
      name: sleepId
      in: path
      required: true
      schema: { type: string }
    MediaId:
      name: mediaId
      in: path
      required: true
      schema: { type: string }
    TaskId:
      name: taskId
      in: path
      required: true
      schema: { type: string }
    ExpenseId:
      name: expenseId
      in: path
      required: true
      schema: { type: string }
    JobId:
      name: jobId
      in: path
      required: true
      schema: { type: string }
    Date:
      name: date
      in: query
      required: true
      schema: { type: string, format: date }
    DateFrom:
      name: from
      in: query
      schema: { type: string, format: date }
    DateTo:
      name: to
      in: query
      schema: { type: string, format: date }
    Limit:
      name: limit
      in: query
      schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
    Cursor:
      name: cursor
      in: query
      schema: { type: string }
    Granularity:
      name: granularity
      in: query
      schema: { type: string, enum: [month, year], default: month }
  responses:
    BadRequest:
      description: Request could not be parsed or validated.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }
    NotFound:
      description: Resource was not found.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }
    Conflict:
      description: Request conflicts with an existing resource identity.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }
    Gone:
      description: Resource was deleted and retained as a tombstone.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }
  schemas:
    Error:
      type: object
      required: [code, message, request_id]
      properties:
        code: { type: string }
        message: { type: string }
        request_id: { type: string }
    SourceKind:
      type: string
      enum: [apple_health_export, gpx, fit, tcx, strava_export]
    UploadSource:
      type: string
      enum: [web, telegram, cli, ios_bridge]
    CreateImport:
      type: object
      required: [raw_file_id, parser_kind]
      properties:
        raw_file_id: { type: string }
        parser_kind:
          type: string
          enum: [apple_health_export, gpx, anilist, bangumi]
    RawFile:
      type: object
      required:
        [
          id,
          sha256,
          original_filename,
          content_type,
          size_bytes,
          source_kind,
          uploaded_via,
          created_at,
        ]
      properties:
        id: { type: string }
        sha256: { type: string }
        original_filename: { type: string }
        content_type: { type: string }
        size_bytes: { type: integer, format: int64 }
        source_kind: { $ref: "#/components/schemas/SourceKind" }
        uploaded_via: { $ref: "#/components/schemas/UploadSource" }
        created_at: { type: string, format: date-time }
        duplicate: { type: boolean }
    ImportJob:
      type: object
      required:
        [id, raw_file_id, status, parser_kind, parser_version, created_at]
      properties:
        id: { type: string }
        raw_file_id: { type: string }
        status: { type: string, enum: [queued, parsing, completed, failed] }
        parser_kind: { type: string }
        parser_version: { type: string }
        error_message: { type: string }
        started_at: { type: string, format: date-time }
        finished_at: { type: string, format: date-time }
        created_at: { type: string, format: date-time }
    Job:
      type: object
      required:
        [
          id,
          kind,
          status,
          attempts,
          max_attempts,
          run_after,
          created_at,
          updated_at,
        ]
      properties:
        id: { type: string }
        kind: { type: string }
        status:
          { type: string, enum: [queued, running, completed, failed, canceled] }
        attempts: { type: integer }
        max_attempts: { type: integer }
        run_after: { type: string, format: date-time }
        error_message: { type: string }
        started_at: { type: string, format: date-time }
        finished_at: { type: string, format: date-time }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    CreateTask:
      type: object
      required: [title]
      properties:
        title: { type: string, minLength: 1 }
        notes: { type: string }
        due_date: { type: string, format: date }
        priority: { type: integer }
    ExpenseItem:
      type: object
      additionalProperties: false
      required: [name]
      properties:
        name: { type: string, minLength: 1 }
        amount_minor: { type: integer, format: int64, minimum: 0 }
    ExpenseSource:
      type: object
      additionalProperties: false
      required: [kind, ref]
      properties:
        kind: { type: string, minLength: 1, maxLength: 50 }
        ref: { type: string, minLength: 1, maxLength: 200 }
    CreateExpense:
      type: object
      additionalProperties: false
      required: [occurred_on, currency, amount_minor, category, source]
      properties:
        occurred_on: { type: string, format: date }
        currency: { type: string, enum: [JPY, USD, EUR, GBP] }
        amount_minor: { type: integer, format: int64, minimum: 1 }
        category: { type: string, enum: [food, groceries, transport, shopping, housing, utilities, health, entertainment, subscriptions, work, other] }
        merchant: { type: string, maxLength: 200 }
        note: { type: string, maxLength: 2000 }
        items:
          type: array
          maxItems: 32
          items: { $ref: "#/components/schemas/ExpenseItem" }
        source: { $ref: "#/components/schemas/ExpenseSource" }
    ReplaceExpense:
      type: object
      additionalProperties: false
      required: [occurred_on, currency, amount_minor, category]
      properties:
        occurred_on: { type: string, format: date }
        currency: { type: string, enum: [JPY, USD, EUR, GBP] }
        amount_minor: { type: integer, format: int64, minimum: 1 }
        category: { type: string, enum: [food, groceries, transport, shopping, housing, utilities, health, entertainment, subscriptions, work, other] }
        merchant: { type: string, maxLength: 200 }
        note: { type: string, maxLength: 2000 }
        items:
          type: array
          maxItems: 32
          items: { $ref: "#/components/schemas/ExpenseItem" }
    Expense:
      type: object
      additionalProperties: false
      required: [id, occurred_on, currency, currency_exponent, amount_minor, category, merchant, note, items, source, created_at, updated_at]
      properties:
        id: { type: string }
        occurred_on: { type: string, format: date }
        currency: { type: string, enum: [JPY, USD, EUR, GBP] }
        currency_exponent: { type: integer, enum: [0, 2] }
        amount_minor: { type: integer, format: int64 }
        category: { type: string, enum: [food, groceries, transport, shopping, housing, utilities, health, entertainment, subscriptions, work, other] }
        merchant: { type: string }
        note: { type: string }
        items: { type: array, items: { $ref: "#/components/schemas/ExpenseItem" } }
        source: { $ref: "#/components/schemas/ExpenseSource" }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    ExpensePage:
      type: object
      additionalProperties: false
      required: [items, next_cursor, has_more]
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/Expense" } }
        next_cursor: { type: [string, "null"] }
        has_more: { type: boolean }
    Task:
      type: object
      required: [id, title, status, priority, source, created_at, updated_at]
      properties:
        id: { type: string }
        title: { type: string }
        notes: { type: string }
        status: { type: string, enum: [open, completed, canceled] }
        due_date: { type: string, format: date }
        priority: { type: integer }
        source: { type: string }
        completed_at: { type: string, format: date-time }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    MediaResolutionTask:
      type: object
      required: [id, task_type, status, candidates, resolution, created_at]
      properties:
        id: { type: string }
        task_type: { type: string, enum: [dedupe_candidate, progress_conflict] }
        status: { type: string, enum: [open, resolved, dismissed] }
        candidates: { type: object, additionalProperties: true }
        resolution: { type: object, additionalProperties: true }
        created_at: { type: string, format: date-time }
        resolved_at: { type: string, format: date-time }
    Activity:
      type: object
      required:
        [
          id,
          sport_type,
          title,
          started_at,
          timezone,
          source_kind,
          first_raw_file_id,
          created_at,
          updated_at,
        ]
      properties:
        id: { type: string }
        sport_type: { type: string }
        title: { type: string }
        started_at: { type: string, format: date-time }
        ended_at: { type: string, format: date-time }
        timezone: { type: string }
        distance_m: { type: number }
        duration_s: { type: integer }
        moving_time_s: { type: integer }
        elevation_gain_m: { type: number }
        avg_hr: { type: integer }
        max_hr: { type: integer }
        avg_pace_s_per_km: { type: number }
        source_kind: { type: string }
        source_activity_id: { type: string }
        first_raw_file_id: { type: string }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    SummaryTotals:
      type: object
      required:
        [
          activity_count,
          distance_m,
          distance_known_count,
          distance_unknown_count,
          duration_s,
          elevation_gain_m,
        ]
      properties:
        activity_count: { type: integer }
        distance_m: { type: number }
        distance_known_count: { type: integer }
        distance_unknown_count: { type: integer }
        duration_s: { type: integer }
        elevation_gain_m: { type: number }
    SummaryBucket:
      allOf:
        - $ref: "#/components/schemas/SummaryTotals"
        - type: object
          required: [key]
          properties:
            key: { type: string }
    ActivitySummary:
      type: object
      required: [totals, by_year, by_month, by_sport]
      properties:
        totals: { $ref: "#/components/schemas/SummaryTotals" }
        by_year:
          { type: array, items: { $ref: "#/components/schemas/SummaryBucket" } }
        by_month:
          { type: array, items: { $ref: "#/components/schemas/SummaryBucket" } }
        by_sport:
          { type: array, items: { $ref: "#/components/schemas/SummaryBucket" } }
    RouteFeatureCollection:
      type: object
      required: [type, features]
      properties:
        type: { type: string, const: FeatureCollection }
        features:
          { type: array, items: { $ref: "#/components/schemas/RouteFeature" } }
    RouteFeature:
      type: object
      required: [type, geometry, properties]
      properties:
        type: { type: string, const: Feature }
        geometry: { $ref: "#/components/schemas/RouteGeometry" }
        properties: { $ref: "#/components/schemas/RouteFeatureProperties" }
    RouteGeometry:
      type: object
      required: [type, coordinates]
      properties:
        type: { type: string, const: LineString }
        coordinates:
          type: array
          items:
            type: array
            minItems: 2
            maxItems: 2
            items: { type: number }
    RouteFeatureProperties:
      type: object
      required: [activity_id, sport_type, year, city, city_status]
      properties:
        activity_id: { type: string }
        sport_type: { type: string }
        year: { type: string }
        city: { type: string }
        city_status: { type: string, enum: [pending, resolved, unknown] }
    ActivityPage:
      allOf: [{ $ref: "#/components/schemas/Page" }]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/Activity" }
    Page:
      type: object
      required: [items, next_cursor, has_more]
      properties:
        items: { type: array, items: {} }
        next_cursor: { type: [string, "null"] }
        has_more: { type: boolean }
    RoutePoint:
      type: object
      required: [seq, lat, lon]
      properties:
        seq: { type: integer }
        ts: { type: string, format: date-time }
        lat: { type: number }
        lon: { type: number }
        elevation_m: { type: number }
        distance_m: { type: number }
        speed_mps: { type: number }
        heart_rate: { type: integer }
    Sampling:
      type: object
      required: [id, sampling_type, ts, value, unit]
      properties:
        id: { type: string }
        sampling_type: { type: string }
        ts: { type: string, format: date-time }
        value: { type: number }
        unit: { type: string }
    Lap:
      type: object
      required: [id, lap_no]
      properties:
        id: { type: string }
        lap_no: { type: integer }
        start_ts: { type: string, format: date-time }
        end_ts: { type: string, format: date-time }
        distance_m: { type: number }
        duration_s: { type: integer }
        avg_hr: { type: integer }
        avg_pace_s_per_km: { type: number }
    Sleep:
      type: object
      required:
        [
          id,
          wake_date,
          started_at,
          ended_at,
          time_in_bed_s,
          asleep_s,
          efficiency,
          is_main_sleep,
          core_s,
          deep_s,
          rem_s,
          awake_s,
          unspecified_s,
          source,
          first_raw_file_id,
          created_at,
          updated_at,
        ]
      properties:
        id: { type: string }
        wake_date: { type: string, format: date }
        started_at: { type: string, format: date-time }
        ended_at: { type: string, format: date-time }
        time_in_bed_s: { type: integer }
        asleep_s: { type: integer }
        efficiency: { type: number }
        is_main_sleep: { type: boolean }
        core_s: { type: integer }
        deep_s: { type: integer }
        rem_s: { type: integer }
        awake_s: { type: integer }
        unspecified_s: { type: integer }
        source: { type: string }
        first_raw_file_id: { type: string }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    SleepPage:
      allOf: [{ $ref: "#/components/schemas/Page" }]
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/Sleep" } }
    SleepSegment:
      type: object
      required: [id, stage, started_at, ended_at, seq]
      properties:
        id: { type: string }
        stage: { type: string }
        started_at: { type: string, format: date-time }
        ended_at: { type: string, format: date-time }
        seq: { type: integer }
    Daily:
      type: object
      required:
        [id, day, ring, source, first_raw_file_id, created_at, updated_at]
      properties:
        id: { type: string }
        day: { type: string, format: date }
        ring:
          type: [object, "null"]
          required:
            [
              move_kcal,
              move_goal_kcal,
              exercise_min,
              exercise_goal_min,
              stand_hours,
              stand_goal_hours,
            ]
          properties:
            move_kcal: { type: number }
            move_goal_kcal: { type: number }
            exercise_min: { type: number }
            exercise_goal_min: { type: number }
            stand_hours: { type: number }
            stand_goal_hours: { type: number }
        steps: { type: number }
        distance_km: { type: number }
        flights: { type: number }
        resting_hr: { type: number }
        walking_hr_avg: { type: number }
        hrv_sdnn: { type: number }
        spo2_avg: { type: number }
        spo2_min: { type: number }
        respiratory_rate: { type: number }
        vo2max: { type: number }
        body_mass_kg: { type: number }
        source: { type: string }
        first_raw_file_id: { type: string }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    DailyPage:
      allOf: [{ $ref: "#/components/schemas/Page" }]
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/Daily" } }
    SleepAggregateResponse:
      type: object
      required: [granularity, buckets]
      properties:
        granularity: { type: string, enum: [month, year] }
        buckets:
          {
            type: array,
            items: { $ref: "#/components/schemas/SleepAggregateBucket" },
          }
    SleepAggregateBucket:
      type: object
      required:
        [
          period,
          session_count,
          main_sleep_count,
          nap_count,
          average_asleep_s,
          average_time_in_bed_s,
          average_efficiency,
          core_s,
          deep_s,
          rem_s,
          awake_s,
          unspecified_s,
        ]
      properties:
        period: { type: string }
        session_count: { type: integer }
        main_sleep_count: { type: integer }
        nap_count: { type: integer }
        average_asleep_s: { type: number }
        average_time_in_bed_s: { type: number }
        average_efficiency: { type: number }
        core_s: { type: integer }
        deep_s: { type: integer }
        rem_s: { type: integer }
        awake_s: { type: integer }
        unspecified_s: { type: integer }
    DailyAggregateResponse:
      type: object
      required: [granularity, buckets]
      properties:
        granularity: { type: string, enum: [month, year] }
        buckets:
          {
            type: array,
            items: { $ref: "#/components/schemas/DailyAggregateBucket" },
          }
    DailyAggregateBucket:
      type: object
      required:
        [
          period,
          days,
          move_kcal_avg,
          exercise_min_avg,
          stand_hours_avg,
          move_closed_pct,
          metrics,
        ]
      properties:
        period: { type: string }
        days: { type: integer }
        move_kcal_avg: { type: number }
        exercise_min_avg: { type: number }
        stand_hours_avg: { type: number }
        move_closed_pct: { type: number }
        metrics:
          {
            type: array,
            items: { $ref: "#/components/schemas/DailyMetricAggregate" },
          }
    DailyMetricAggregate:
      type: object
      required: [metric, value, unit, observed_days]
      properties:
        metric: { type: string }
        value: { type: number }
        unit: { type: string }
        observed_days: { type: integer }
    Media:
      type: object
      required:
        [id, title, media_type, item_role, last_update_at, hidden_from_continue]
      properties:
        id: { type: string }
        title: { type: string }
        media_type: { type: string }
        item_role: { type: string }
        cover_image_url: { type: string }
        status: { type: string }
        position: { type: number }
        total: { type: number }
        unit: { type: string }
        progress_percent: { type: number }
        last_update_at: { type: string, format: date-time }
        rating: { type: number }
        hidden_from_continue: { type: boolean }
        native_title: { type: string }
        episode_count: { type: integer }
        chapter_count: { type: integer }
    MediaPage:
      allOf: [{ $ref: "#/components/schemas/Page" }]
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/Media" } }
        status_counts: { type: object, additionalProperties: { type: integer } }
        active_count: { type: integer }
    MediaEventPage:
      allOf: [{ $ref: "#/components/schemas/Page" }]
      properties:
        items:
          {
            type: array,
            items: { $ref: "#/components/schemas/MediaHomeEvent" },
          }
    MediaDetail:
      type: object
      required: [item, work, creators, relations, events]
      properties:
        item: { $ref: "#/components/schemas/Media" }
        work: { $ref: "#/components/schemas/MediaWork" }
        progress: { $ref: "#/components/schemas/MediaProgress" }
        creators:
          { type: array, items: { $ref: "#/components/schemas/MediaCreator" } }
        relations:
          { type: array, items: { $ref: "#/components/schemas/MediaRelation" } }
        events:
          { type: array, items: { $ref: "#/components/schemas/MediaEvent" } }
    MediaWork:
      type: object
      required:
        [
          id,
          work_kind,
          primary_title,
          original_title,
          original_language,
          description,
        ]
      properties:
        id: { type: string }
        work_kind: { type: string }
        primary_title: { type: string }
        original_title: { type: string }
        original_language: { type: string }
        first_release_date: { type: string, format: date-time }
        description: { type: string }
    MediaProgress:
      type: object
      required: [status, unit, play_count]
      properties:
        status: { type: string }
        unit: { type: string }
        position: { type: number }
        total: { type: number }
        progress_percent: { type: number }
        started_at: { type: string, format: date-time }
        last_update_at: { type: string, format: date-time }
        finished_at: { type: string, format: date-time }
        play_count: { type: integer }
    MediaEvent:
      type: object
      required: [id, event_type]
      properties:
        id: { type: string }
        event_type: { type: string }
        event_at: { type: string, format: date-time }
        unit: { type: string }
        position: { type: number }
        total: { type: number }
        progress_percent: { type: number }
        rating: { type: number }
        note: { type: string }
    MediaHomeEvent:
      type: object
      required: [id, media_id, title, event_type, occurred_at]
      properties:
        id: { type: string }
        media_id: { type: string }
        title: { type: string }
        native_title: { type: string }
        cover_image_url: { type: string }
        event_type: { type: string }
        occurred_at: { type: string, format: date-time }
        unit: { type: string }
        position: { type: number }
        total: { type: number }
        progress_percent: { type: number }
        rating: { type: number }
    MediaCreator:
      type: object
      required: [id, name, role]
      properties:
        id: { type: string }
        name: { type: string }
        role: { type: string }
    MediaRelation:
      type: object
      required:
        [
          id,
          relation_type,
          direction,
          related_item_id,
          related_title,
          related_type,
        ]
      properties:
        id: { type: string }
        relation_type: { type: string }
        direction: { type: string }
        related_item_id: { type: string }
        related_title: { type: string }
        related_type: { type: string }
        cover_image_url: { type: string }
    MediaAggregates:
      type: object
      required: [totals, completions_by_year, score_distribution, type_split]
      properties:
        totals: { $ref: "#/components/schemas/MediaTotals" }
        completions_by_year:
          {
            type: array,
            items: { $ref: "#/components/schemas/MediaCompletionBucket" },
          }
        score_distribution:
          {
            type: array,
            items: { $ref: "#/components/schemas/MediaScoreBucket" },
          }
        type_split:
          {
            type: array,
            items: { $ref: "#/components/schemas/MediaTypeBucket" },
          }
    MediaTotals:
      type: object
      required:
        [item_count, completed_count, this_year_completed, average_rating]
      properties:
        item_count: { type: integer }
        completed_count: { type: integer }
        this_year_completed: { type: integer }
        average_rating: { type: number }
    MediaCompletionBucket:
      type: object
      required: [year, count]
      properties:
        year: { type: integer }
        count: { type: integer }
    MediaScoreBucket:
      type: object
      required: [score, count]
      properties:
        score: { type: number }
        count: { type: integer }
    MediaTypeBucket:
      type: object
      required: [type, count]
      properties:
        type: { type: string }
        count: { type: integer }
    Briefing:
      type: object
      required: [date, previous_date, next_date, sections]
      properties:
        date: { type: string, format: date }
        previous_date: { type: string, format: date }
        next_date: { type: string, format: date }
        sections:
          { type: array, items: { type: object, additionalProperties: true } }
