// Read-only client that fetches from iroha-server at runtime. Render entirely
// in the browser (SPA); no prerendering or server-side data loading.
export const ssr = false;
export const prerender = false;
