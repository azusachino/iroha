node_modules

# Regenerated by `make public-site-data`/`public-site-build` from a live DB
# immediately before every build (see Makefile) -- never committed, so the
# repo never carries a real or stale snapshot of personal activity data.
/static/data

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
