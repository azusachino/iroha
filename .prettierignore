# Root prettier scope: docs + config only (md/yaml/json). The web app runs its
# OWN prettier (spaces + double quotes, prettier-plugin-svelte) via `make web-fmt`
# with apps/iroha-web/.prettierrc; Go, SQL, and Python use their own toolchains.
node_modules/
.git/
apps/iroha-web/
.iroha-data/
**/dist/
**/build/
**/.svelte-kit/
